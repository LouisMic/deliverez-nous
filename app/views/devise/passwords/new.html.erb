<h2 class="text-center mt-3">Vous avez oublié votre mot de passe ?</h2>

<div class="container offset-4 col-4 mt-3">

<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email,
                label: "Courriel",
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
  </div>

  <div class="form-actions d-grid gap-2">
    <%= f.button :submit, "Renouveler mon mot de passe par courriel", class: "btn btn-lg btn-block button-orange" %>
  </div>
<% end %>

<div class="form-actions mt-3 text-center">
<%= render "devise/shared/links" %>
</div>
</div>
