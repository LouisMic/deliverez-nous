
<div class="backhome" >
  <div class="logo">
  </div>

  <%# <%= link_to "Se connecter", "#", class: "button-login" %>

  <%# <%= link_to "S'enregistrer", "#", class: "button-signin" %>


    <div class="homecontainer" >
      <div class="formcard border shadow">
      <div class="d-flex justify-content-center">
      <%= image_tag asset_url("logo1b.png")%>
      </div>

        <%= simple_form_for @booking do |f| %>
          <%= f.input :address, input_html: {value: "Ville ou adresse"} , label: false %>
          <%# remplacer plus tard et importer le vrai array %>
          <%= f.input :category, collection:[['concert', 'concert'], ['magie', 'magie'], ['théâtre', 'théâtre'], ['spectacle enfant', 'spectacle enfant'], ['mentalist', 'mentalist']], prompt: "Catégorie", label: false %>
          <%= f.input :date, label: false, :order => [:day, :month, :year] %>
          <%= f.submit 'Rechercher', class:"button-orange" %>
        <% end %>
      </div>
    </div>
</div>
<%# <div class="homefooter"%>
<%# </div> %>
<%# style="background-image: url(<%= asset_url("grande_banner.png")%>
